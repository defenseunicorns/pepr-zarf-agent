package main

import (
	"flag"
	"fmt"
	"log"
	"net"

	pb "github.com/cmwylie19/pepr-zarf-agent/transformer/api/v1/image"
	"github.com/cmwylie19/pepr-zarf-agent/transformer/pkg/server"
	"google.golang.org/grpc"
	"google.golang.org/grpc/reflection"
)

/*
 * Simple, Secure, gRPC Transformer Server
 */
var (
	// tls        = flag.Bool("tls", false, "Connection uses TLS if true, else plain TCP")
	// certFile   = flag.String("cert_file", "", "The TLS cert file")
	// keyFile    = flag.String("key_file", "", "The TLS key file")
	// jsonDBFile = flag.String("json_db_file", "", "A json file containing a list of features")
	port = flag.Int("port", 50051, "The server port")
)

func main() {
	flag.Parse()
	lis, err := net.Listen("tcp", fmt.Sprintf(":%d", *port))
	if err != nil {
		log.Fatalf("failed to listen: %v", err)
	}
	// var opts []grpc.ServerOption
	// if *tls {
	// 	if *certFile == "" {
	// 		*certFile = data.Path("x509/server_cert.pem")
	// 	}
	// 	if *keyFile == "" {
	// 		*keyFile = data.Path("x509/server_key.pem")
	// 	}
	// 	creds, err := credentials.NewServerTLSFromFile(*certFile, *keyFile)
	// 	if err != nil {
	// 		log.Fatalf("Failed to generate credentials: %v", err)
	// 	}
	// 	opts = []grpc.ServerOption{grpc.Creds(creds)}
	// }
	grpcServer := grpc.NewServer()
	reflection.Register(grpcServer) // not recommended in prod
	pb.RegisterImageTransformServer(grpcServer, &server.ImageTransformServer{})
	log.Printf("gRPC Transformer Server listening on port %v", lis.Addr())
	if err := grpcServer.Serve(lis); err != nil {
		log.Fatalf("failed to serve: %v", err)
	}
}
